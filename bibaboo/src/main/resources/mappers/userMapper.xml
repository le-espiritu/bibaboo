<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="user">

	<select id="getUserByEmail">
		SELECT id, email, password, name, phone_number, create_date, enable
		FROM user
		WHERE email = #{loginUserEmail}
	</select>
	
	<select id="getUserRoleByEmail">
		SELECT ur.id, ur.user_id, ur.role_name
		FROM user_role ur
		JOIN user u
		ON ur.user_id = u.id
		WHERE u.email = #{loginUserEmail}
	</select>

</mapper>